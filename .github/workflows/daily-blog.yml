# =============================================================================
# BIOBUILDS Daily Blog Post Generator — v4 (Multi-language)
# =============================================================================
#
# Architecture:
#   Three sequential Claude runs produce the SAME topic adapted for each locale:
#   1. EN: Picks topic, writes English version (international audience)
#   2. DE: Reads EN article, adapts for DACH (Germany/Austria focus)
#   3. RO: Reads EN article, adapts for Romanian audience
#   All git operations are deterministic workflow steps after Claude finishes.
#
# SETUP:
#   1. Run `claude setup-token` locally → copy the token
#   2. Repo → Settings → Secrets → Actions → New repository secret:
#      Name:  CLAUDE_CODE_OAUTH_TOKEN
#      Value: (paste token from step 1)
#   3. Place this file at: .github/workflows/daily-blog.yml
#   4. Merge to default branch (schedule only fires from default branch)
#   5. Test: Actions tab → "Daily Blog Post" → Run workflow
#
# SECURITY:
#   - Actions pinned to full 40-char commit SHAs
#   - Permissions: contents:write + pull-requests:write + id-token:write
#   - OAuth token = high-value credential → rotate every 60–90 days
#   - Trigger: schedule + workflow_dispatch only (no PR/fork triggers)
# =============================================================================

name: Daily Blog Post

on:
  schedule:
    - cron: '0 2 * * *'           # 2:00 UTC ≈ 04:00 EET / 05:00 EEST
  workflow_dispatch:                # manual trigger from Actions tab

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 1: Write English article (picks the topic)
  # ═══════════════════════════════════════════════════════════════════════════
  write-en:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: Generate English article
        uses: anthropics/claude-code-action@01e756b34ef7a1447e9508f674143b07d20c2631
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          allowed_tools: "Read,Write,Edit,Glob,Grep,WebFetch,WebSearch"

          prompt: |
            You are the BIOBUILDS blog content engine.
            Your job: write ONE new blog post in ENGLISH and save it to disk.

            ██  DO NOT run any git commands.
            ██  DO NOT create branches, commits, or pull requests.

            ═══════════════════════════════════════════════════════════════
            PHASE 1 — RECONNAISSANCE
            ═══════════════════════════════════════════════════════════════

            1. Read CLAUDE.md for complete instructions
            2. Read SEO.md for keyword strategy and content rules
            3. Read SOURCES.md — your research database
            4. Count articles per pillar in EN/ directory
            5. Pick the pillar with the FEWEST articles
            6. Read 2-3 existing EN articles to match format exactly
            7. Read blog-styles.css — use ONLY existing classes

            ═══════════════════════════════════════════════════════════════
            PHASE 2 — RESEARCH
            ═══════════════════════════════════════════════════════════════

            8. Pick 3-5 sources from SOURCES.md relevant to your topic
            9. Use WebFetch to extract real data from those sources
            10. Use WebSearch for 1-2 fresh 2025-2026 sources
            11. Cross-reference statistics across sources

            ═══════════════════════════════════════════════════════════════
            PHASE 3 — WRITE ENGLISH VERSION
            ═══════════════════════════════════════════════════════════════

            12. Pick a topic that:
                - Is NOT already covered by existing articles
                - Targets a keyword from SEO.md
                - Has sufficient source material

            13. Write the article in ENGLISH:
                - Match existing article format exactly
                - Include Schema.org JSON-LD markup
                - Use only bb-* CSS classes from blog-styles.css
                - 2,000-3,000 words
                - Every statistic backed by a source
                - International/EU perspective (can mention all markets)

            14. Save to EN/ directory using naming convention:
                {Pillar}.{SubArticle} EN {url-slug}.html

            15. ALSO create a small metadata file at topic-of-day.json:
                {"topic": "brief topic description", "pillar": "3", "slug": "the-url-slug"}

            ═══════════════════════════════════════════════════════════════
            RULES
            ═══════════════════════════════════════════════════════════════
            - NEVER run git commands
            - NEVER invent statistics
            - NEVER use CSS classes not in blog-styles.css
            - NEVER duplicate existing topics

          claude_args: |
            --max-turns 25
            --model claude-sonnet-4-5-20250929

      - name: Upload EN workspace
        uses: actions/upload-artifact@v4
        with:
          name: workspace-en
          path: |
            EN/
            DE/
            RO/
            topic-of-day.json
          retention-days: 1

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 2: Write German article (adapt from EN for DACH audience)
  # ═══════════════════════════════════════════════════════════════════════════
  write-de:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: write-en

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: Download EN workspace
        uses: actions/download-artifact@v4
        with:
          name: workspace-en
          path: .

      - name: Generate German article
        uses: anthropics/claude-code-action@01e756b34ef7a1447e9508f674143b07d20c2631
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          allowed_tools: "Read,Write,Edit,Glob,Grep,WebFetch,WebSearch"

          prompt: |
            You are the BIOBUILDS blog content engine.
            Your job: write the GERMAN version of today's blog post, adapted for DACH audience.

            ██  DO NOT run any git commands.
            ██  DO NOT create branches, commits, or pull requests.

            ═══════════════════════════════════════════════════════════════
            PHASE 1 — FIND TODAY'S TOPIC
            ═══════════════════════════════════════════════════════════════

            1. Read topic-of-day.json to find today's topic and pillar
            2. Find and read the NEW English article in EN/ directory
               (look for the most recently created file matching the topic)
            3. Read CLAUDE.md for formatting requirements
            4. Read 2-3 existing DE articles to match German tone/style
            5. Read blog-styles.css for CSS classes

            ═══════════════════════════════════════════════════════════════
            PHASE 2 — ADAPT FOR DACH AUDIENCE
            ═══════════════════════════════════════════════════════════════

            6. Translate and ADAPT the English article for German-speaking
               audience in Germany and Austria:

               LOCALIZATION REQUIREMENTS:
               - Write in fluent, professional German (not machine translation)
               - Focus on Germany and Austria markets ONLY
               - DO NOT mention Romania, Bulgaria, or Eastern European markets
               - Use German/Austrian regulations (GEG, KfW, OIB)
               - Reference German/Austrian incentive programs
               - Use € pricing with German/Austrian market context
               - Cite German-language sources where available (.de, .at domains)
               - Adapt examples to German/Austrian building practices
               - Use formal "Sie" address throughout

               KEEP THE SAME:
               - Overall article structure and sections
               - Core technical content and statistics
               - BIOBUILDS value propositions
               - Schema.org markup structure (update inLanguage to "de")
               - CSS classes (bb-* classes)

            ═══════════════════════════════════════════════════════════════
            PHASE 3 — WRITE AND SAVE
            ═══════════════════════════════════════════════════════════════

            7. Write the complete German article:
                - Same pillar number as EN version
                - German title and meta description
                - All content in German
                - 2,000-3,000 words
                - Update all Schema.org to inLanguage: "de"

            8. Save to DE/ directory using naming convention:
                {Pillar}.{SubArticle} DE {german-url-slug}.html

            ═══════════════════════════════════════════════════════════════
            RULES
            ═══════════════════════════════════════════════════════════════
            - NEVER run git commands
            - NEVER mention Romania or Eastern European context
            - NEVER use CSS classes not in blog-styles.css
            - Write natural German, not literal translation

          claude_args: |
            --max-turns 25
            --model claude-sonnet-4-5-20250929

      - name: Upload DE workspace
        uses: actions/upload-artifact@v4
        with:
          name: workspace-de
          path: |
            EN/
            DE/
            RO/
            topic-of-day.json
          retention-days: 1

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 3: Write Romanian article (adapt from EN for Romanian audience)
  # ═══════════════════════════════════════════════════════════════════════════
  write-ro:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: write-de

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: Download DE workspace
        uses: actions/download-artifact@v4
        with:
          name: workspace-de
          path: .

      - name: Generate Romanian article
        uses: anthropics/claude-code-action@01e756b34ef7a1447e9508f674143b07d20c2631
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          allowed_tools: "Read,Write,Edit,Glob,Grep,WebFetch,WebSearch"

          prompt: |
            You are the BIOBUILDS blog content engine.
            Your job: write the ROMANIAN version of today's blog post, adapted for Romanian audience.

            ██  DO NOT run any git commands.
            ██  DO NOT create branches, commits, or pull requests.

            ═══════════════════════════════════════════════════════════════
            PHASE 1 — FIND TODAY'S TOPIC
            ═══════════════════════════════════════════════════════════════

            1. Read topic-of-day.json to find today's topic and pillar
            2. Find and read the NEW English article in EN/ directory
            3. Read CLAUDE.md for formatting requirements
            4. Read 2-3 existing RO articles to match Romanian tone/style
            5. Read blog-styles.css for CSS classes

            ═══════════════════════════════════════════════════════════════
            PHASE 2 — ADAPT FOR ROMANIAN AUDIENCE
            ═══════════════════════════════════════════════════════════════

            6. Translate and ADAPT the English article for Romanian audience:

               LOCALIZATION REQUIREMENTS:
               - Write in fluent, professional Romanian
               - Focus on Romania market primarily
               - Can reference EU context and standards
               - Use Romanian regulations and building codes
               - Reference Romanian incentive programs (Casa Verde, etc.)
               - Use € pricing but with Romanian market context
               - Compare to Romanian average construction costs
               - Cite Romanian sources where available (.ro domains)
               - Adapt examples to Romanian building practices
               - Mention BIOBUILDS factory is in Romania (local advantage)

               KEEP THE SAME:
               - Overall article structure and sections
               - Core technical content and statistics
               - BIOBUILDS value propositions
               - Schema.org markup structure (update inLanguage to "ro")
               - CSS classes (bb-* classes)

            ═══════════════════════════════════════════════════════════════
            PHASE 3 — WRITE AND SAVE
            ═══════════════════════════════════════════════════════════════

            7. Write the complete Romanian article:
                - Same pillar number as EN version
                - Romanian title and meta description
                - All content in Romanian
                - 2,000-3,000 words
                - Update all Schema.org to inLanguage: "ro"

            8. Save to RO/ directory using naming convention:
                {Pillar}.{SubArticle} RO {romanian-url-slug}.html

            ═══════════════════════════════════════════════════════════════
            RULES
            ═══════════════════════════════════════════════════════════════
            - NEVER run git commands
            - NEVER use CSS classes not in blog-styles.css
            - Write natural Romanian, not literal translation
            - Emphasize local factory advantage where relevant

          claude_args: |
            --max-turns 25
            --model claude-sonnet-4-5-20250929

      - name: Upload final workspace
        uses: actions/upload-artifact@v4
        with:
          name: workspace-final
          path: |
            EN/
            DE/
            RO/
          retention-days: 1

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 4: Commit all articles and create PR
  # ═══════════════════════════════════════════════════════════════════════════
  commit-and-pr:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: write-ro

    env:
      BRANCH_NAME: blog/auto-${{ github.run_id }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: Create working branch
        run: git checkout -b "$BRANCH_NAME"

      - name: Download final workspace
        uses: actions/download-artifact@v4
        with:
          name: workspace-final
          path: .

      - name: Check for changes
        id: changes
        run: |
          STATUS="$(git status --porcelain)"
          if [ -z "$STATUS" ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            echo "::warning::No new files detected"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            echo "✅ New content detected:"
            echo "$STATUS"
          fi

      - name: Commit and push
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "blog: auto-generated posts $(date +%Y-%m-%d) [EN/DE/RO]"
          git push origin "$BRANCH_NAME"

      - name: Create Pull Request
        if: steps.changes.outputs.has_changes == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh auth status || echo "$GITHUB_TOKEN" | gh auth login --with-token

          EXISTING_PR=$(gh pr list --head "$BRANCH_NAME" --json number --jq '.[0].number' 2>/dev/null || true)

          if [ -n "$EXISTING_PR" ]; then
            echo "ℹ️ PR #$EXISTING_PR already exists"
            exit 0
          fi

          FILE_LIST=$(git show --name-only --pretty="" HEAD)

          gh pr create \
            --title "📝 Blog: $(date +%Y-%m-%d) [EN/DE/RO]" \
            --body "$(cat <<EOF
          ## Auto-generated blog posts (3 languages)

          **Branch:** \`${BRANCH_NAME}\`
          **Generated:** $(date -u +%Y-%m-%dT%H:%M:%SZ)

          ### Languages
          - 🇬🇧 English (international)
          - 🇩🇪 German (DACH market)
          - 🇷🇴 Romanian (local market)

          ### Changed files
          \`\`\`
          ${FILE_LIST}
          \`\`\`

          ---
          *Review all 3 versions before merging. Generated by BIOBUILDS blog engine.*
          EOF
          )" \
            --base "${{ github.event.repository.default_branch }}" \
            --head "$BRANCH_NAME"
